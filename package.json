{
  "name": "nido-card-battle",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "simulate": "vitest run src/game/simulation.report.test.ts --mode simulation --reporter=verbose",
    "battlelog": "vite-node tools/generate-battle-log.mjs"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vite-node": "^3.2.4",
    "vitest": "^3.2.4"
  }
}
