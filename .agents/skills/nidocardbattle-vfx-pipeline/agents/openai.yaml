interface:
  display_name: "NidoCardBattle VFX Pipeline"
  short_description: "Reuse and build lightweight tactical combat FX"
  default_prompt: "Use $nidocardbattle-vfx-pipeline to reuse the accepted WorldXplore VFX language and integrate the smallest readable NidoCardBattle effect."
policy:
  allow_implicit_invocation: true
